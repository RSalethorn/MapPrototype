class App extends React.Component {
    render(){
        return(
            <p>Map Test</p>
        );
    }
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App/>);